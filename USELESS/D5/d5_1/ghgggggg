#include<iostream>

using namespace std;

int arr[100][100];

int main()
{
  ios_base::sync_with_stdio(0);
  cin.tie(0); cout.tie(0);

  for(int i = 0;i<10;i++)
  {
    for(int j = 0;j<10;j++)
    {
      arr[i][j] = i*10+j;
    }
  }
  
  for(int i = 0;i<10;i++)
  {
    for(int j = 0;j<10;j++)
    {
      cout<<arr[i][j]<<" ";
    }
    cout<<"\n";
  }
  return 0;
}