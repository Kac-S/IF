#include<iostream>

using namespace std;

int main()
{
  int a;
  cin>>a;
  for(int i = 1;i<a+1;i++)
  { 
    if(i%2 == 0)
    {      
      if(i%3 == 0)
      {cout<<"Fizz ";}
      else
      {cout<<i<<" ";}
    }
  }

  return 0;
}