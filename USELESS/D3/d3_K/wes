#include<iostream>
using namespace std;
 
int NWD(int a, int b)
{
   while(a!=b)
    if(a>b)
        a-=b;
    else
        b-=a;
   return a;
}
 
int main()
{
  ios_base::sync_with_stdio(0);
  cin.tie(0); cout.tie(0);
  
  int z, n, d;;
  cin >> z;

  while (z--)
  {
    cin >> n >> d;
    cout << n << endl;
  }
  return 0;
}
