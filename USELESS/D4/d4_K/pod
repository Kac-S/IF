#include<iostream>
 
using namespace std;
 
int main()
{
 ios_base::sync_with_stdio(0);
 cin.tie(0); cout.tie(0);
 
 long int a, b;
 long int k;
 cin >> a >> b >> k;
 
 long int n = 0;
 
 for (int i = a; i <= b; i++)
 {
   if(i % k == 0)
   {
      n ++;
   }
 }
 cout << n;
 
 return 0;
}